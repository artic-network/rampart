
##### Config file #####

# configfile: "config.yaml" #absolute path from annotation.js

##### Target rules #####

rule all:
    input:
        expand(config["output_path"]+ "/{filename_stem}.csv", filename_stem=config["filename_stem"])

##### Modules #####
include: "rules/demultiplex.smk"
include: "rules/map.smk"

